# 已确认的项目决策

以下为你已经拍板的结果。未落地到代码的仅剩「生产域名 / `NEXT_PUBLIC_SITE_URL`」，因为这需要你的最终域名值。

## 品牌与叙事

- **站点标题 / OG siteName**：保持中性默认值，不改。  
- **首页 Hero 标签文案**：保持当前写法，不替换。  
- **GitHub 链接**：保持 `https://github.com/xmustu`，不改。

## 内容与合规

- **ingest 源与频率**：先保持当前配置，不改。  
- **403 / Jina 回退**：开启，作为推荐默认项。  
- **默认语言策略**：保持当前 **默认中文 + `as-needed`**，不调整。

## 工程与部署

- **生产域名与 `NEXT_PUBLIC_SITE_URL`**：待你提供最终域名后填写；RSS 绝对链接、sitemap、canonical 都依赖它。  
- **公开模板定位**：确定为 yes；仓库需写清 **License**、**展示用 Demo 部署**、以及 **不收集访客 PII** 的说明。

## 可选增强（未实现）

- Monorepo 中单独 **Nextra 文档子站**（与主站双部署）。  
- 博客正文 **目录（TOC）**、**阅读进度条**、**代码复制按钮**（需客户端组件与 AST 处理）。  
- **全文搜索**（Algolia / Orama 等，涉及成本与索引策略）。